# ---------------------------------------------------------------------------
# Secrets
# ---------------------------------------------------------------------------
# `.env.example` is committed and is the only env file that ever should be.
.env
.env.*
!.env.example

# ---------------------------------------------------------------------------
# Python
# ---------------------------------------------------------------------------
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
env/
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml

# ---------------------------------------------------------------------------
# Node / web
# ---------------------------------------------------------------------------
node_modules/
web/dist/
web/.vite/
*.tsbuildinfo

# Playwright output. `web/tests/**` (the specs) is committed; the run artefacts are not.
test-results/
playwright-report/
blob-report/
.playwright/
playwright/.cache/

# ---------------------------------------------------------------------------
# Local tooling state
# ---------------------------------------------------------------------------
.claude/
CLAUDE.md
.specify/integrations/claude.manifest.json
.omni/

# `plans/` is published. An earlier pass excluded everything under it except GUARDRAILS.md as
# "internal planning docs"; that was reversed deliberately -- CLAUDE.md and README.md both link
# to plans/PLAN-00-OVERVIEW.md and the phase docs, and a repository whose own entry point 404s
# is worse than one that shows its working. The phase docs are also the clearest evidence of
# how the build was sequenced.

# ---------------------------------------------------------------------------
# Editors and OS
# ---------------------------------------------------------------------------
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
desktop.ini
